@model IEnumerable<Prepod.Models.students_group>

<p>
    @Html.ActionLink("Create New", "Create")
</p>

                <table class="table table-striped table-bordered cell-size" id="TableListOfStudentPrak">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.id_students_group)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.id_group)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.name_s)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.id_students_group)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.id_group)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.name_s)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
    }
</table>
<div class="modal-footer">
    <button id="CancelGroup" class="btn btn-danger" type="button" data-dismiss="modal"> Отмена</button>
    <button id="ClearGroup" class="btn btn-info" type="button" data-dismiss="modal">Очистить</button>
    <button id="AddGroup" class="btn btn-success" type="button" data-dismiss="modal">Принять</button>
</div>




      


